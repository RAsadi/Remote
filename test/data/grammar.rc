fn do_something() {
  return;
}

fn name(x: u32, y: u32) -> u32 {
  let x: u32 = y;
  let mut y = x;
  if x {
    let z = 10;
  } else if y {
    1 * 1 / 2 << 2;
  } else {
    y = 2;
  }
  while y {
    do_something();
    /*
    Uhoh multiline comment
    */
  }
  return y;
}
// Heres a comment;
fn main() -> u32 {
  return name(1, 2);
}
